profiles {

    local {
        process{
		executor = 'local'
	}
    }

    cluster {
        process{
		executor = 'sge'
                queueSize = 100
	}
    }

}

// Enable trace by default
trace.enabled = true
