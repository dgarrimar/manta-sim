profiles {

    local {
        executor{
	        name = 'local'
	}
    }

    cluster {        
	executor{
		name = 'crg'
                queueSize = 300
	}
    }
}

process{
    container = 'dgarrimar/simreal:r3.5.2'
    withName: kinship {
        memory = '90G'
    }
}

docker {
    sudo = false
    fixOwnership = true
    runOptions = '-u $(id -u)'
}

singularity {
    cacheDir = 'singularity'
    autoMounts = true
}
